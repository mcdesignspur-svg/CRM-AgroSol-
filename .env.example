# PostgreSQL — copia a .env y ajusta según tu entorno
DATABASE_URL="postgresql://agrosol:agrosol@localhost:5432/agrosol_crm?schema=public"

# Neon (u otro proveedor con connection pooling):
#
# DATABASE_URL — URL pooled (host con "-pooler"), usada por la app en runtime.
# En Vercel, configúrala en Production (y Preview si aplica).
#
# URL directa (sin "-pooler") para migraciones: el build de producción
# (scripts/vercel-build.mjs) usa, en orden, DIRECT_DATABASE_URL,
# DATABASE_URL_UNPOOLED o POSTGRES_URL_NON_POOLING (las dos últimas las expone
# automáticamente la integración de Neon en Vercel). Las migraciones necesitan
# conexión directa: los advisory locks de Prisma Migrate fallan con timeout
# P1002 a través del pooler. Los builds de preview NO ejecutan migraciones.
#
# Ejemplo Neon:
# DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
# DIRECT_DATABASE_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
